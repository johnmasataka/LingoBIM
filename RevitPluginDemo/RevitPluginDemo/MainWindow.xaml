<Window x:Class="RevitPluginDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LingoBIM" Height="300" Width="400">
    <Grid>
        <!-- 输入框 -->
        <TextBox Name="CommandInput" HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="350" Margin="10,10,0,0" TextWrapping="Wrap" AcceptsReturn="True" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" TextChanged="CommandInput_TextChanged"/>

        <!-- 提交按钮 -->
        <Button Content="Send Prompt" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Height="30" Margin="10,0,0,10" Click="SendCommand_Click"/>

        <!-- 指令提示 -->
        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="350" Margin="10,0,0,50" Text="Hi there, Let's LingoModel!"/>
    </Grid>
</Window>
